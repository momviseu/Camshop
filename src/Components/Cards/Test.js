import React from 'react'

const Test = (data) => {

  return (
    <div>Test
        {/* {
            data.map((item, i) =>{
                return (
                    <div key={i}>
                        <p>{item.title}</p>
                    </div>
                )
            })
        } */}
    </div>
  )
}

export default Test;